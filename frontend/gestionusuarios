<!-- ...existing code... -->
<table>
    <thead>
        <!-- ...existing code... -->
        <th>Acciones</th>
    </thead>
    <tbody>
        <!-- Example row -->
        <tr>
            <!-- ...existing code... -->
            <td>
                <button onclick="editarUsuario('userId')">Editar</button>
                <button onclick="borrarUsuario('userId')">Borrar</button>
            </td>
        </tr>
        <!-- ...existing code... -->
    </tbody>
</table>
<script>
    function editarUsuario(userId) {
        // Lógica para editar el usuario
        console.log(`Editar usuario: ${userId}`);
    }

    function borrarUsuario(userId) {
        // Lógica para borrar el usuario
        console.log(`Borrar usuario: ${userId}`);
    }
</script>
<!-- ...existing code... -->